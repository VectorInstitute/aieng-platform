# =============================================================================
# LOCAL DEVELOPMENT ENVIRONMENT VARIABLES
# =============================================================================

# Google Cloud
GCP_PROJECT_ID=coderd
FIRESTORE_DATABASE_ID=onboarding
GCS_BUCKET_NAME=coder-analytics-snapshots

# Google OAuth
# NOTE: Reuses same OAuth client as onboarding-status-web
# Get these from Google Cloud Console OAuth 2.0 Client
# Make sure to add the analytics redirect URI to the same OAuth client:
#   https://your-domain.com/analytics/api/auth/callback
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret

# Session Security
# NOTE: Reuses same secret as onboarding-status-web
# Cookies are scoped to different paths (/analytics vs /onboarding) so they won't conflict
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-session-secret-at-least-32-characters

# Application URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
REDIRECT_URI=http://localhost:3000/analytics/api/auth/callback

# Access Control
# Comma-separated list of allowed email domains
ALLOWED_DOMAINS=vectorinstitute.ai

# =============================================================================
# GITHUB SECRETS (For CI/CD Deployment)
# Set these in: https://github.com/your-org/your-repo/settings/secrets/actions
# =============================================================================

# GCP_PROJECT_ID=coderd
# Already set above, used in workflows

# WIF_PROVIDER=projects/PROJECT_NUMBER/locations/global/workloadIdentityPools/POOL_ID/providers/PROVIDER_ID
# Workload Identity Federation provider for GitHub Actions authentication to GCP

# GCP_SERVICE_ACCOUNT=github-actions@PROJECT_ID.iam.gserviceaccount.com
# Service account email for Cloud Run deployments

# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# Same as NEXT_PUBLIC_GOOGLE_CLIENT_ID but as GitHub secret
# REUSED from onboarding-status-web (same OAuth client)

# GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret
# OAuth client secret (keep secure)
# REUSED from onboarding-status-web (same OAuth client)

# SESSION_SECRET=your-32-char-or-longer-secret
# Session encryption key (keep secure)
# REUSED from onboarding-status-web (cookies scoped to different paths)

# APP_URL=https://your-domain.com
# Production application URL (without /analytics path)

# REDIRECT_URI=https://your-domain.com/analytics/api/auth/callback
# OAuth redirect URI for production

# CODER_TOKEN=your-coder-session-token
# Coder API token for data collection workflow
# Get this by running: coder tokens create --lifetime 8760h
# Or from Coder UI: Settings > Account > Tokens
